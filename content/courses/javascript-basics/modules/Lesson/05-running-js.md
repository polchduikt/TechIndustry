# Running JavaScript: как запускать JavaScript

JavaScript можно запускать **разными способами** — не только в браузере.
Понимание того, **где и как выполняется JS-код**, важно для каждого разработчика.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, где может выполняться JavaScript
- запускать JavaScript в браузере
- запускать JavaScript с помощью Node.js

---

## 🧠 Теория

### Где выполняется JavaScript
JavaScript может выполняться в двух основных средах:

- 🌐 **В браузере**
- 🖥 **На сервере (Node.js)**

Хотя язык один и тот же, среда выполнения отличается.

---

### Запуск JavaScript в браузере
Самый распространённый способ — запуск JS в браузере.

JavaScript можно подключить к HTML-файлу:

```html
<script src="script.js"></script>
```

Или написать прямо в HTML:

```html
<script>
  console.log("JavaScript работает в браузере");
</script>
```

📌 Браузер автоматически выполняет этот код при загрузке страницы.

---

### Запуск через Console
JavaScript можно запускать **напрямую через DevTools**.

1. Открой DevTools
2. Перейди во вкладку Console
3. Введи код:

```js
2 + 2
```

👉 Результат появится сразу.

---

### Запуск JavaScript с помощью Node.js
Node.js позволяет запускать JavaScript **вне браузера**.

Пример файла `app.js`:

```js
console.log("Hello from Node.js");
```

Запуск в терминале:

```bash
node app.js
```

📌 Этот способ часто используется для:
- серверных приложений
- скриптов
- автоматизации

---

### Отличия браузера и Node.js
| Браузер | Node.js |
|-------|--------|
| Есть DOM | Нет DOM |
| Работа с HTML | Работа с файлами |
| Используется для UI | Используется для серверов |

---

## 💻 Простой пример

```js
let name = "JavaScript";
console.log(`Running ${name}`);
```

Этот код будет работать **и в браузере, и в Node.js**.

---

## 📝 Итоги
- JavaScript можно запускать разными способами
- Браузер — основной способ для веба
- Node.js — для серверов и скриптов
- Среда выполнения влияет на возможности JS
