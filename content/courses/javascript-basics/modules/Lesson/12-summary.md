# JavaScript Summary: краткий итог

Этот урок — **обобщение всех основных тем**, которые ты уже изучил.
Он помогает структурировать знания и увидеть общую картину JavaScript.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- вспомнить ключевые концепции JavaScript
- связать темы между собой
- лучше понимать, как писать простой JS-код

---

## 🧠 Основные понятия

### Как работает JavaScript
- JavaScript выполняется **последовательно**
- Он является **однопоточным**
- Выполнение кода контролируется **Call Stack**

---

### Запуск JavaScript
JavaScript можно запускать:
- в браузере
- в DevTools (Console)
- с помощью Node.js

Одна и та же логика может работать в разных средах.

---

### Переменные и типы данных
Для хранения данных используются переменные:

- `let` — изменяемые значения
- `const` — неизменяемые ссылки

Типы данных делятся на:
- примитивные
- ссылочные (объекты, массивы, функции)

---

### Операторы и условия
Операторы позволяют:
- выполнять вычисления
- сравнивать значения
- строить логику

Условия (`if`, `else`, `switch`) помогают программе принимать решения.

---

### Циклы
Циклы используются для повторения действий:

- `for`
- `while`
- `do...while`

С их помощью удобно работать с массивами и данными.

---

### Функции
Функции позволяют:
- переиспользовать код
- разбивать программу на части
- передавать и возвращать данные

Существуют:
- обычные функции
- function expressions
- arrow functions

---

## 💻 Пример объединения знаний

```js
function calculateSum(numbers) {
  let sum = 0;

  for (let i = 0; i < numbers.length; i++) {
    sum += numbers[i];
  }

  return sum;
}

console.log(calculateSum([1, 2, 3, 4]));
```

Этот пример использует:
- функцию
- цикл
- переменные
- операторы

---

## 📝 Итоги
- JavaScript состоит из простых, но важных концепций
- Все темы связаны между собой
- Понимание основ — ключ к изучению сложных тем
- Практика важнее заучивания

