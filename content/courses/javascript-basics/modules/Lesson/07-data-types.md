# Data Types in JavaScript: типы данных

Типы данных определяют, **какую именно информацию** хранит переменная
и какие операции можно с ней выполнять.

JavaScript — язык с **динамической типизацией**, поэтому понимание типов данных особенно важно.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- знать основные типы данных в JavaScript
- отличать примитивные и ссылочные типы
- понимать, как JavaScript работает с типами

---

## 🧠 Теория

### Динамическая типизация
В JavaScript тип данных определяется **во время выполнения**.

```js
let value = 10;
value = "text";
```

📌 Тип переменной может изменяться — это нормально для JavaScript.

---

### Примитивные типы данных
Примитивы — это простые значения.

Основные примитивные типы:

- `number`
- `string`
- `boolean`
- `null`
- `undefined`
- `symbol`
- `bigint`

---

### Number
Тип `number` используется для чисел.

```js
let age = 25;
let price = 99.9;
```

📌 В JavaScript **нет отдельного типа для целых и дробных чисел**.

---

### String
`string` — это строка текста.

```js
let name = "Alex";
let message = `Hello, ${name}`;
```

Строки можно писать:
- в кавычках `" "`
- в одинарных `' '`
- в шаблонных `` ` ` ``

---

### Boolean
Тип `boolean` имеет два значения:

```js
true
false
```

Используется в условиях и логике программы.

---

### null и undefined
- `undefined` — значение не присвоено
- `null` — значение отсутствует намеренно

```js
let a;
let b = null;
```

---

### Symbol и BigInt
- `symbol` — уникальное значение
- `bigint` — для очень больших чисел

```js
const id = Symbol("id");
const big = 12345678901234567890n;
```

---

### Ссылочные типы данных
Ссылочные типы хранятся по ссылке.

Основные:
- `object`
- `array`
- `function`

---

### Object
```js
const user = {
  name: "John",
  age: 30
};
```

Объекты позволяют хранить связанные данные.

---

### Array
```js
const numbers = [1, 2, 3];
```

Массивы — это упорядоченные коллекции данных.

---

## 💻 Пример сравнения

```js
let x = 10;
let y = x;

y = 20;

console.log(x); // 10
console.log(y); // 20
```

```js
const obj1 = { value: 10 };
const obj2 = obj1;

obj2.value = 20;

console.log(obj1.value); // 20
```

📌 Примитивы копируются по значению, объекты — по ссылке.

---

## 📝 Итоги
- JavaScript имеет динамическую типизацию
- Примитивы и объекты работают по-разному
- Тип данных влияет на поведение кода
- Понимание типов — ключ к чистому JS-коду

---

👉 Следующие важные темы:
- type coercion
- typeof
- сравнение значений 🚀
