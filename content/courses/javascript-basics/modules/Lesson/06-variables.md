# Variables in JavaScript: переменные

Переменные — это основа любого JavaScript-кода.
Они позволяют **хранить данные**, работать с ними и изменять их во время выполнения программы.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, что такое переменные
- использовать `var`, `let` и `const`
- выбирать правильный тип объявления переменной

---

## 🧠 Теория

### Что такое переменная
**Переменная** — это контейнер для хранения данных.

Проще говоря:
> переменная — это имя, за которым скрывается значение

Пример:
```js
let age = 20;
```

Здесь:
- `age` — имя переменной
- `20` — значение

---

### Объявление переменных
В JavaScript есть три способа объявить переменную:

- `var`
- `let`
- `const`

---

### var (устаревший способ)
`var` — старый способ объявления переменных.

```js
var name = "Alex";
```

Минусы `var`:
- нет блочной области видимости
- может приводить к ошибкам

📌 Сейчас `var` **почти не используется**.

---

### let
`let` — современный способ объявления переменных.

```js
let city = "Kyiv";
city = "Lviv";
```

Особенности:
- можно менять значение
- есть блочная область видимости

---

### const
`const` используется для значений, которые **не должны изменяться**.

```js
const PI = 3.14;
```

📌 Попытка изменить `const` приведёт к ошибке.

Важно:
- `const` защищает **ссылку**, а не данные внутри объекта

---

### Пример с объектом
```js
const user = {
  name: "John"
};

user.name = "Mike"; // ✅ работает
```

---

## 💻 Пример использования

```js
let score = 0;
score = score + 10;

const gameName = "JS Game";

console.log(score);
console.log(gameName);
```

---

## 📝 Итоги
- Переменные хранят данные
- `let` — для изменяемых значений
- `const` — для неизменяемых ссылок
- `var` лучше избегать
