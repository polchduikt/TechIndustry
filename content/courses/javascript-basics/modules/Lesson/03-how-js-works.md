# How JavaScript Works

JavaScript — это однопоточный язык программирования, который выполняет код последовательно.
Понимание того, как он работает «под капотом», помогает писать более надежный и понятный код.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понять, как выполняется JavaScript-код
- объяснить, что такое Call Stack
- понять, почему JavaScript называют однопоточным

---

## 🧠 Теория

### Однопоточность JavaScript
JavaScript выполняет **одну операцию за раз**.
Он читает и выполняет код **строка за строкой**, сверху вниз.

Это означает:
- нет параллельного выполнения кода
- каждая команда должна завершиться, прежде чем начнется следующая

---

### Выполнение кода
Пример обычного синхронного кода:

```js
console.log("Start");
console.log("Process");
console.log("End");
```

Результат в консоли:
```
Start
Process
End
```

Код выполняется строго по порядку.

---

### Call Stack
**Call Stack** — это механизм, который отслеживает выполнение функций.

Проще говоря:
> Call Stack — это стек вызовов функций

Он работает по принципу **LIFO** (Last In — First Out).

---

### Пример работы Call Stack

```js
function greet() {
  sayHello();
}

function sayHello() {
  console.log("Hello!");
}

greet();
```

Порядок выполнения:
1. `greet()` добавляется в стек
2. `sayHello()` добавляется в стек
3. выполняется `console.log`
4. `sayHello()` удаляется из стека
5. `greet()` удаляется из стека

---

### Ошибка переполнения стека

```js
function loop() {
  loop();
}

loop();
```

Результат:
```
Maximum call stack size exceeded
```

Причина — бесконечный вызов функций.

---

## 📝 Итоги
- JavaScript — однопоточный язык
- Код выполняется последовательно
- Call Stack управляет выполнением функций
- Переполнение стека — частая ошибка новичков
