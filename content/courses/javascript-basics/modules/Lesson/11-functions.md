# Functions in JavaScript: функции

Функции позволяют **группировать код** и использовать его многократно.
Это один из самых важных инструментов в JavaScript.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, что такое функция
- объявлять и вызывать функции
- использовать параметры и возвращаемые значения

---

## 🧠 Теория

### Что такое функция
**Функция** — это блок кода, который выполняет определённую задачу
и может быть вызван в любом месте программы.

Пример:
```js
function sayHello() {
  console.log("Hello!");
}
```

---

### Вызов функции
Чтобы код внутри функции выполнился, её нужно вызвать:

```js
sayHello();
```

📌 Функция может вызываться **сколько угодно раз**.

---

### Параметры функции
Функции могут принимать данные — **параметры**.

```js
function greet(name) {
  console.log("Hello, " + name);
}

greet("Alex");
```

Здесь `name` — параметр функции.

---

### Возврат значения
Функция может возвращать результат с помощью `return`.

```js
function sum(a, b) {
  return a + b;
}

let result = sum(3, 5);
```

📌 После `return` выполнение функции прекращается.

---

### Function Expression
Функцию можно записать в переменную.

```js
const multiply = function(a, b) {
  return a * b;
};
```

Такие функции часто используются в современном JavaScript.

---

### Arrow Functions
Стрелочные функции — короткий синтаксис функций.

```js
const add = (a, b) => a + b;
```

📌 Arrow functions делают код компактнее.

---

## 💻 Пример использования

```js
function isAdult(age) {
  return age >= 18;
}

console.log(isAdult(20)); // true
console.log(isAdult(15)); // false
```

---

## 📝 Итоги
- Функции позволяют переиспользовать код
- Параметры передают данные
- `return` возвращает результат
- Arrow functions — современный синтаксис

---

👉 Следующие темы:
- scope функций
- callback функции
- функции высшего порядка 🚀
