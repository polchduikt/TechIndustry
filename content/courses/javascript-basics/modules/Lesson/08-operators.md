# Operators in JavaScript: операторы

Операторы в JavaScript позволяют **выполнять операции над значениями**:
складывать, сравнивать, присваивать и управлять логикой программы.

Без операторов невозможно написать ни одной реальной программы.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, что такое операторы
- использовать основные типы операторов
- читать и писать выражения в JavaScript

---

## 🧠 Теория

### Что такое оператор
**Оператор** — это специальный символ или слово,
которое выполняет операцию над одним или несколькими значениями.

Пример:
```js
let sum = 5 + 3;
```

Здесь `+` — оператор сложения.

---

### Арифметические операторы
Используются для работы с числами.

Основные арифметические операторы:

- `+` сложение
- `-` вычитание
- `*` умножение
- `/` деление
- `%` остаток от деления
- `**` возведение в степень

Пример:
```js
let result = 10 % 3; // 1
```

---

### Операторы присваивания
Используются для присваивания значений переменным.

```js
let x = 10;
x += 5; // 15
x *= 2; // 30
```

📌 Такие операторы делают код короче и понятнее.

---

### Операторы сравнения
Операторы сравнения возвращают `true` или `false`.

Основные:
- `==` нестрогое равенство
- `===` строгое равенство
- `!=` неравенство
- `!==` строгое неравенство
- `>` `<` `>=` `<=`

Пример:
```js
5 === "5"; // false
5 == "5";  // true
```

📌 Рекомендуется использовать **строгое сравнение (`===`)**.

---

### Логические операторы
Используются для работы с условиями.

- `&&` И
- `||` ИЛИ
- `!` НЕ

Пример:
```js
let isAdult = age >= 18 && hasPassport;
```

---

### Унарные операторы
Работают с одним значением.

```js
let count = 1;
count++;
count--;
```

---

### Тернарный оператор
Короткая форма `if...else`.

```js
let access = age >= 18 ? "allowed" : "denied";
```

📌 Делает код компактным, но не стоит злоупотреблять.

---

## 💻 Пример использования

```js
let a = 10;
let b = 5;

let sum = a + b;
let isEqual = a === b;

console.log(sum);
console.log(isEqual);
```

---

## 📝 Итоги
- Операторы выполняют действия над данными
- Арифметические — для чисел
- Сравнение — для условий
- Логические — для сложной логики
- `===` безопаснее чем `==`

