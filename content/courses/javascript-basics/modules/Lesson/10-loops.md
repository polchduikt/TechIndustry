# Loops in JavaScript: циклы

Циклы позволяют **выполнять один и тот же код несколько раз**.
Они используются, когда нужно обработать коллекции данных или повторяющиеся действия.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, зачем нужны циклы
- использовать `for`, `while` и `do...while`
- контролировать выполнение цикла

---

## 🧠 Теория

### Что такое цикл
**Цикл** — это конструкция, которая повторяет выполнение кода,
пока выполняется заданное условие.

Пример:
```js
for (let i = 0; i < 3; i++) {
  console.log(i);
}
```

---

### Цикл for
`for` используется, когда **известно количество повторений**.

```js
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

Структура:
- инициализация
- условие
- шаг

---

### Цикл while
`while` выполняется, пока условие истинно.

```js
let count = 0;

while (count < 3) {
  console.log(count);
  count++;
}
```

📌 Если условие сразу `false`, цикл не выполнится ни разу.

---

### Цикл do...while
`do...while` похож на `while`, но выполняется **минимум один раз**.

```js
let number = 0;

do {
  console.log(number);
  number++;
} while (number < 3);
```

---

### Управление циклом
JavaScript позволяет управлять выполнением цикла.

- `break` — полностью останавливает цикл
- `continue` — пропускает текущую итерацию

Пример:
```js
for (let i = 0; i < 5; i++) {
  if (i === 2) continue;
  if (i === 4) break;
  console.log(i);
}
```

---

### Циклы и массивы
Циклы часто используются для перебора массивов.

```js
const numbers = [10, 20, 30];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}
```

---

## 💻 Пример использования

```js
let sum = 0;

for (let i = 1; i <= 5; i++) {
  sum += i;
}

console.log(sum);
```

---

## 📝 Итоги
- Циклы повторяют выполнение кода
- `for` — когда известно количество повторений
- `while` — когда важно условие
- `do...while` выполняется минимум один раз
- `break` и `continue` управляют циклом

