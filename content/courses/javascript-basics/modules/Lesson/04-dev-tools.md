# DevTools: инструменты разработчика в браузере

DevTools (Developer Tools) — это встроенные инструменты в браузере,
которые помогают разработчику **изучать, отлаживать и улучшать веб‑приложения**.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, зачем нужны DevTools
- ориентироваться в основных вкладках
- использовать DevTools для отладки JavaScript

---

## 🧠 Теория

### Что такое DevTools
DevTools — это набор инструментов, встроенных в браузер (Chrome, Edge, Firefox).

С их помощью можно:
- смотреть HTML и CSS страницы
- отслеживать ошибки JavaScript
- анализировать производительность
- работать с сетью и запросами

Открываются они сочетанием клавиш:
```
F12
```
или
```
Ctrl + Shift + I
```

---

### Вкладка Elements
Вкладка **Elements** показывает структуру страницы (HTML) и стили (CSS).

Здесь можно:
- просматривать HTML‑элементы
- временно менять CSS
- проверять, какие стили применяются к элементу

📌 Изменения в этой вкладке **не сохраняются** после перезагрузки страницы.

---

### Вкладка Console
**Console** — главный инструмент для работы с JavaScript.

Она позволяет:
- выводить сообщения (`console.log`)
- видеть ошибки
- выполнять JavaScript‑код прямо в браузере

Пример:
```js
console.log("Hello from DevTools");
```

Если в коде есть ошибка, она сразу появится в Console.

---

### Вкладка Sources
Вкладка **Sources** используется для отладки кода.

Здесь можно:
- просматривать JavaScript‑файлы
- ставить breakpoints (точки остановки)
- пошагово выполнять код

📌 Это особенно полезно для поиска сложных ошибок.

---

### Вкладка Network
**Network** показывает все сетевые запросы страницы.

С её помощью можно:
- видеть запросы к серверу
- проверять ответы (JSON, статус коды)
- анализировать скорость загрузки

👉 Часто используется при работе с API.

---

## 💻 Пример использования

1. Открой страницу в браузере
2. Запусти DevTools
3. Перейди во вкладку Console
4. Выполни команду:

```js
alert("DevTools работает!");
```

---

## 📝 Итоги
- DevTools — основной инструмент веб‑разработчика
- Console помогает отлаживать JavaScript
- Elements — работать с HTML и CSS
- Network — анализировать запросы
- Sources — искать ошибки в коде

---

👉 Умение работать с DevTools экономит часы разработки
и делает отладку в разы быстрее 🚀
