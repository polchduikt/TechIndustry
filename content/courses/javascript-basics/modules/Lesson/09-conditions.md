# Conditions in JavaScript: условия

Условия позволяют программе **принимать решения**.
С их помощью JavaScript выполняет разный код в зависимости от ситуации.

Без условий невозможно создать логику, которая реагирует на действия пользователя.

---

## 🎯 Цель урока
После этого урока ты сможешь:
- понимать, как работают условия
- использовать `if`, `else if`, `else`
- применять `switch` для нескольких вариантов

---

## 🧠 Теория

### Что такое условие
**Условие** — это проверка, результатом которой является `true` или `false`.

Пример:
```js
age >= 18
```

Если условие истинно — выполняется один код, если ложно — другой.

---

### if
Оператор `if` выполняет код, **если условие истинно**.

```js
if (age >= 18) {
  console.log("Access granted");
}
```

Если условие `false`, код внутри блока не выполнится.

---

### else
`else` выполняется, если условие в `if` ложно.

```js
if (age >= 18) {
  console.log("Access granted");
} else {
  console.log("Access denied");
}
```

---

### else if
Используется, когда есть **несколько условий**.

```js
if (score >= 90) {
  grade = "A";
} else if (score >= 75) {
  grade = "B";
} else {
  grade = "C";
}
```

📌 Проверка идёт сверху вниз — выполнится первый подходящий блок.

---

### switch
`switch` — альтернатива множеству `else if`.

```js
switch (day) {
  case "Monday":
    console.log("Start of week");
    break;
  case "Friday":
    console.log("Almost weekend");
    break;
  default:
    console.log("Regular day");
}
```

Важно:
- `break` останавливает выполнение
- `default` выполняется, если ни один case не подошёл

---

### Truthy и Falsy
В условиях JavaScript автоматически приводит значения к `true` или `false`.

Falsy значения:
- `false`
- `0`
- `""`
- `null`
- `undefined`
- `NaN`

Все остальные значения — `truthy`.

---

## 💻 Пример использования

```js
let isLoggedIn = true;

if (isLoggedIn) {
  console.log("Welcome!");
} else {
  console.log("Please log in");
}
```

---

## 📝 Итоги
- Условия управляют логикой программы
- `if / else` — основной инструмент
- `switch` удобен для множества вариантов
- Truthy / Falsy важно понимать для условий
